name: LinkedIn Scheduler (disabled)

# Disabled 2026-08-10: LinkedIn tooling removed from the MCP server.
# This workflow is inert. Delete this file (and the /api/linkedin/cron
# route it called) once you're ready to clean up.

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "LinkedIn scheduler disabled — safe to delete this workflow file."
