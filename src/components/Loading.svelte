<script lang="ts">
</script>

<div
    class="fixed text-8xl tracking-widest top-0 right-0 bottom-0 left-0 z-50 flex items-center h-screen text-gray-700 justify-center bg-gray-900"
    id="loading"
>
    <p class="absolute animate-pulse">please wait</p>
    <!-- <p
        class={`absolute ${
            loadingPercentNum < 20
                ? "bottom-20"
                : loadingPercentNum < 50
                ? "bottom-40"
                : loadingPercentNum < 80
                ? "bottom-3/4"
                : "bottom-full"
        }  right-12`}
    > -->
    <!-- </p> -->
</div>
